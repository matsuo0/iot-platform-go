# Server Configuration
SERVER_PORT=8080
SERVER_HOST=localhost

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=iot_platform
DB_USER=postgres
DB_PASSWORD=password
DB_SSL_MODE=disable

# MQTT Configuration
MQTT_BROKER=tcp://localhost:1883
MQTT_CLIENT_ID=iot-platform-server
MQTT_USERNAME=
MQTT_PASSWORD=

# JWT Configuration
JWT_SECRET=your-secret-key-here
JWT_EXPIRATION=24h

# Logging
LOG_LEVEL=info 